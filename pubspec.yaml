name: material_themes_manager
description: An easier and standardized way to manage flutter app themes.
version: 1.3.0
homepage: https://github.com/hoverdroids/Flutter-Material-Themes-Manager

environment:
  sdk: '>=3.3.0-71.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: